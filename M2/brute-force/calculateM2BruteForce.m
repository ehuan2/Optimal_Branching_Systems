% includes -----------------------------------------------------------------------------------------
addpath('M2/brute-force/SetPartFolder/SetPartFolder/')

% functions ----------------------------------------------------------------------------------------

% this function takes a graph G, and produces M2 through brute force
function [M2] = calculateM2BruteForce(G)
  % G is fed in as an n x n adjacency matrix

  % strategy:
  %  
  % [_, Arref, incidenceMatrix, transformedA] = calculateM2(G);
  

end

